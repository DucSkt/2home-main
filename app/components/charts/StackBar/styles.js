import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1
  },
  detailStack: {
    justifyContent: "center",
    alignItems: "center",
    padding: 10
  }
});
