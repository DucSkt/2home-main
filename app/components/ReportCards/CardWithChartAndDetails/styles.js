import { StyleSheet } from 'react-native';
import { Colors } from '../../../themes';

export default StyleSheet.create({
  container: {
    padding: 0
  },

  itemTitleStyle: {
    paddingLeft: 5,
    paddingRight: 5
  },

  itemContentStyle: {
    color: Colors.orange,
    paddingLeft: 5,
    paddingRight: 5
  }
});
