import { StyleSheet } from "react-native";
import { Colors } from "../../themes";

export const tintColorTab = {
  activeTintColor: Colors.orange,
  inactiveTintColor: Colors.frost
};

export const styles = StyleSheet.create({
  icon: {
    width: 25,
    height: 25
  }
});
